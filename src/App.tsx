// src/App.tsx
import React from 'react';
import MainRoutes from './routes/MainRoutes'; // Importing the routing component

const App: React.FC = () => {
  return (
    <div className="App">
    
      <MainRoutes />
    </div>
  );
};

export default App;
